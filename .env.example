# Copy this file contents into Render's "Add from .env" or into a local `.env` (do not commit secrets)
# Replace placeholder values with your real credentials in the hosting UI only.

# MongoDB
MONGO_URI=(SET_IN_RENDER_UI)
MONGO_DB_NAME=odrive_app
MONGO_TLS_ALLOW_INVALID_CERTS=0
MONGO_TLS_CA_FILE=

# Sessions / Redis
USE_REDIS=1
REDIS_URL=(SET_IN_RENDER_UI)
SESSION_SECRET=(SET_IN_RENDER_UI)

# App runtime
PORT=5174
NODE_ENV=production

# Capture / app specific
CAPTURE_MAX_AGE_MINUTES=60
DB_TYPE=mongo

# Google service account (if used)
# Provide the service account values in the hosting UI (Render) instead of
# placing the full private key in the repo. Use `(SET_IN_RENDER_UI)` as a
# placeholder here so pre-commit secret scanners do not trigger.
GOOGLE_CLIENT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="(SET_IN_RENDER_UI)"

# Redis (recommended for production)
REDIS_URL=redis://<password>@<redis-host>:6379
USE_REDIS=true
